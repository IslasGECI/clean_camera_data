services:
  develop:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: islasgeci/clean_camera_data_develop:latest
    command: bash
    volumes:
      - $PWD:/workdir
  islasgeci:
    build:
      context: .
      dockerfile: Dockerfile
    image: islasgeci/clean_camera_data:latest
    command: bash
    volumes:
      - $PWD:/workdir
